<%
	# Downcase all information
	dom = domain.downcase
	host = hostname.downcase
	fhost = name.downcase
%>

#
# <%= fhost %>
[<%= fhost %>] # linux box 
	address <%= munin_host_real %>
	port <%= munin_port_real %>
	use_node_name yes
